package cn.behring.network

import androidx.compose.material.Button
import androidx.compose.material.Text
import androidx.compose.runtime.Composable

@Composable
fun NetworkButton() {
    Button(onClick = { }) {
        Text(text = "NetworkButton")
    }
}